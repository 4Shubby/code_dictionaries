*#git/github commands



### process to add to another local directory from github

1. cd ~ /Desktop/directory to clone into
2. git clone website_of_repository local_name
3. cd ~ /Desktop/directory cloned
4. git init
5. change file 
6. git add “filename”
7. git commit -m “update message”
8. git push -u origin master


###process if a local(pc) is updated and want to push to remote(github)

1. get into directory
## cd ~/Desktop/directory_name

2. check status of updated file to be sure it is being tracked

3. 
##git status
###process

create a directory with code in it on pc

1. get into file from command line
## cd ~/Desktop/directory

2. actualize git for file
## git init

3. add file
## git add “name_of_file”

3b.check status
## git status

4. commit file to repo
## git commit -m “file_name”

4b. check status
##git status  

5. connect to github,com (may not be necessary)
## git remote add origin https://github.com/4Shubby/Sports_analytics.git

6. push file up
## git push -u origin master


###creates directory on pc
mkdir ~/ProjectName   	


###open directory on pc
cd ~/ProjectName	


###makes it github ready
git init		


### to see what version is installed  1.9.3
git version 	  	

### to see all configurations
git config —list  

	 
### to get helper
git config - - global credential.helper osxkeychain    


### to remove changes
git checkout projectname   


### add changes
git add file.name
git add *     ###add all changes
  

### adds a remote rep and gives it a short name
git remote add ea web name.git  
git fetch ea


 ### if want to undo merge
git merge - - abort 



### get github updates into local
git pull origin master


###switching to a brach
git checkout -b name

### lists all names of remotes
git ls -remote origin/eu_working


